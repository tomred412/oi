#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <fstream>

using namespace std;

int main() {

float n = 0;
float average = 0;

  for (int i = 0; i < 12; i++) {
    cin >> n;
    average = n + average;

  }
average = average/12;
cout << "$" << average << endl;

return 0;
}
